services:
  bot:
    build: .
    restart: always
    env_file: .env
    cap_add:
      - SYS_ADMIN
    devices:
      - /dev/fuse
    volumes:
      - ollama-data:/root/.ollama
      - rclone-cache:/tmp/rclone

volumes:
  ollama-data:
  rclone-cache:
